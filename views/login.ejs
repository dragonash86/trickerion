<% include include/header.ejs %>
	<div class="container login">
		<form action="/loginForm" method="post">
			<fieldset>
				<p style="text-align:center;font-size:30px"><strong>공장잼(FactoryFun)</strong></p>
				<p style="text-align:right;padding-top:10px">베타 1.0</p><br /><br />
				<legend>로그인</legend>
		    	<label for="userId">아이디</label><input type="text" id="userId" value="" name="username" autocomplete="off" placeholder="아이디" />
		    	<label for="userPw">비밀번호</label><input type="password" id="userPw" value="" name="password" autocomplete="off" placeholder="비밀번호" />
		    	<input type="submit" class="btn" value="로그인" />
		    	<ul>
		        	<!-- <li><a href="#n">아이디</a>ㆍ<a href="#n">비밀번호 찾기</a></li> -->
		        	<li><a href="/join">회원가입</a></li>
				</ul>
			</fieldset>
		</form>
    </div>
	<a href="/boards" class="btn">게시판가기</a>
    <script>
		$(function() {
			function validation() {
				if ($("#userId").val() === "") {
					$("#userId").focus();
					return;
				} if ($("#userPw").val() === "") {
					$("#userPw").focus();
					return;
				} 
				$("form").submit();
			}
			$("input").keydown(function(key) {
				if(key.keyCode == 13) validation();
			});
			$(".login .btn").click(function(e) {
				e.preventDefault();
				validation();
			});
		});
	</script>
<% include include/footer.ejs %>

